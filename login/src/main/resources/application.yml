spring:
  datasource:
    url: jdbc:mysql://localhost:3306/oauth_login
    username: root
    password: Hin7253a94

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

    spring:
      security:
        oauth2:
          client:
            registration:


              github:
                client-id:
                  client-secret:
                    scope:
                      - read-user:
                      - user-email:

